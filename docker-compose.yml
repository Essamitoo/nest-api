version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: essam
      POSTGRES_PASSWORD: essam123
      POSTGRES_DB: nestdb
    ports:
      - "5499:5432"